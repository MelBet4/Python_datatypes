#Given a tuple of numbers `(10, 20, 30, 40, 50)`, create a 
#new tuple containing only the **first and last elements**.

numbers = (10, 20, 30, 40, 50)
nums = (numbers[0], numbers[-1])
print("New tuple: ", nums)

